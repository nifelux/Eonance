(()=>{const $=s=>document.querySelector(s),money=n=>`₦${Number(n||0).toLocaleString('en-NG',{minimumFractionDigits:2,maximumFractionDigits:2})}`,toast=t=>{const x=document.createElement('div');x.className='toast';x.textContent=t;document.body.append(x);setTimeout(()=>x.remove(),3200)};
async function session(){const c=await window.EonanceSupabase;return (await c?.auth.getSession())?.data?.session}
async function api(action,method='GET',body){const s=await session();if(!s){location.href='/index.html';return null}const r=await fetch(`/api/eonance?action=${action}`,{method,headers:{'Content-Type':'application/json',Authorization:`Bearer ${s.access_token}`},body:body?JSON.stringify(body):undefined});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.detail?`${j.error}: ${j.detail}`:(j.error||'Request failed'));return j}
function shell(content){return `<header class="topbar"><a class="brand" href="/dashboard.html"><span class="brand-mark">E</span><span>EONANCE<small>PRIVATE CAPITAL</small></span></a><div class="top-actions"><a class="text-button" href="/dashboard.html">Dashboard</a></div></header><main class="page"><div class="shell"><aside class="side-nav"><a href="/dashboard.html">Overview</a><a href="/products.html">Packages</a><a class="active" href="/recharge.html">Deposit</a><a href="/withdraw.html">Withdraw</a><a href="/team.html">Team</a><a href="/activity.html">Ledger</a></aside><div class="main-grid">${content}</div></div></main>`}
async function start(){if(!await session())return;$('#app').innerHTML=shell(`<header class="heading"><p class="eyebrow">FUND DEPOSIT BALANCE</p><h1>Reserve a timed transfer.</h1><p>After reserving, you will receive private payment details and a narration for a ten-minute verification window.</p></header><div class="form-layout"><form class="form-card" id="deposit-form"><label>Deposit amount<input name="amount" type="number" min="3000" value="3000" required></label><button class="gold-button">Continue to payment details</button></form><aside class="protocol"><p class="eyebrow">PAYMENT PROTOCOL</p><ol><li>Reserve your transfer details.</li><li>Pay the exact amount within ten minutes.</li><li>Use the narration exactly as shown.</li><li>Wait for administrator verification.</li></ol><p class="eyebrow">MINIMUM DEPOSIT ₦3,000</p></aside></div>`);$('#deposit-form').onsubmit=async e=>{e.preventDefault();const b=e.submitter;b.disabled=true;try{const amount=Number(new FormData(e.target).get('amount'));const d=await api('create-deposit','POST',{amount});const expiresAt=d.expires_at||new Date(Date.now()+10*60*1000).toISOString();localStorage.setItem(`eonance-deposit:${d.reference}`,JSON.stringify({reference:d.reference,narration:d.narration,amount,bank:d.bank||{},expires_at:expiresAt,status:'pending'}));location.href=`/payment.html?reference=${encodeURIComponent(d.reference)}`}catch(x){toast(x.message);b.disabled=false}}}
function timeText(ms){const total=Math.max(0,Math.ceil(ms/1000)),m=Math.floor(total/60),s=total%60;return `${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`}
async function payment(){if(!await session())return;const reference=new URLSearchParams(location.search).get('reference');if(!reference){location.href='/recharge.html';return}let timer,poll;const stored=()=>{try{return JSON.parse(localStorage.getItem(`eonance-deposit:${reference}`)||'null')}catch{return null}};const draw=async()=>{let d;try{d=await api(`deposit-status&reference=${encodeURIComponent(reference)}`)}catch(error){const reservation=stored();if(!reservation)throw error;d={deposit:reservation,bank:reservation.bank||{},expired:new Date(reservation.expires_at).getTime()<=Date.now(),cached:true}}if(!d)return;const deposit=d.deposit,expires=new Date(deposit.expires_at).getTime();const approved=deposit.status==='completed';const rejected=deposit.status==='rejected';if(!d.cached)localStorage.setItem(`eonance-deposit:${reference}`,JSON.stringify({...deposit,bank:d.bank||{}}));$('#app').innerHTML=shell(`<header class="heading"><p class="eyebrow">PRIVATE PAYMENT DESK</p><h1>${approved?'Deposit approved.':rejected?'Deposit rejected.':d.expired?'Payment window elapsed.':'Complete your transfer.'}</h1><p>${approved?'Your verified deposit has been credited to Deposit Balance. Redirecting you to the dashboard.':rejected?'This reservation was rejected. Create a new reservation if you still need to fund your account.':d.cached?'Your payment instruction is retained securely on this device while we recheck its verification status.':'Transfer the exact amount with the narration below. The status checks automatically while the approval window is open.'}</p></header><section class="vault"><span class="vault-label">${approved?'VERIFIED DEPOSIT':d.expired?'RESERVATION EXPIRED':'APPROVAL WINDOW'}</span><strong class="vault-total" id="countdown">${approved?'APPROVED':d.expired?'00:00':'--:--'}</strong><div class="instruction-grid"><span>Bank<b>${d.bank.bank_name||'—'}</b></span><span>Account name<b>${d.bank.account_name||'—'}</b></span><span>Account number<b>${d.bank.account_number||'—'}</b></span><span>Amount<b>${money(deposit.amount)}</b></span><span>Narration<b>${deposit.narration}</b></span><span>Reference<b>${deposit.reference}</b></span></div><button class="outline-button" id="copy-payment">Copy narration</button></section><section class="page-card"><p class="eyebrow">STATUS</p><h2 id="payment-status">${approved?'Approved — Deposit Balance credited':rejected?'Rejected — contact concierge if needed':d.expired?'Expired — reserve new payment details':d.cached?'Instruction secured — verifying reservation status':'Awaiting administrator approval'}</h2></section>`);$('#copy-payment').onclick=()=>navigator.clipboard?.writeText(deposit.narration).then(()=>toast('Narration copied')).catch(()=>toast('Copy the narration manually'));clearInterval(timer);clearInterval(poll);if(approved){localStorage.removeItem(`eonance-deposit:${reference}`);setTimeout(()=>location.href='/dashboard.html',3000);return}if(rejected||d.expired)return;const refresh=()=>{const left=expires-Date.now(),c=$('#countdown');if(c)c.textContent=timeText(left);if(left<=0){clearInterval(timer);draw().catch(e=>toast(e.message))}};refresh();timer=setInterval(refresh,1000);poll=setInterval(()=>draw().catch(e=>toast(e.message)),8000)};await draw()}
window.addEventListener('DOMContentLoaded',()=>{const p=document.body.dataset.page||'';if(p==='deposit-start')start().catch(e=>{$('#app').innerHTML=`<div class="loader">${e.message}</div>`});if(p==='payment')payment().catch(e=>{$('#app').innerHTML=`<div class="loader">${e.message}</div>`})})})();
